# Test Environment Variables
# This file is loaded during test runs via vitest.setup.ts

# Test database (separate from dev database)
DATABASE_URL="file:./prisma/test.db"

# Better Auth configuration for tests
BETTER_AUTH_SECRET="test-secret-key-for-testing-do-not-use-in-production"
BETTER_AUTH_URL="http://localhost:3000"

# Node environment
NODE_ENV="test"

# Skip CRON_SECRET requirement in tests (we'll test auth separately)
# CRON_SECRET is intentionally not set here - tests will verify behavior with and without it
